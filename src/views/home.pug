doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Noom
    link(rel="stylesheet" href="https://unpkg.com/mvp.css@1.12/mvp.css")
    link(rel="stylesheet" href="../public/css.css")
  body
    header
      h1 Noom
    main
      div#welcome
        form
          input(placeholder="room name", required, type="text")#roomname
          input(placeholder="nickname", required, type="text")#nickname
          button Enter
      div#call
        div#myStream
          div#select
            select#cameras(size="4")
          video#myFace(autoplay, playsinline, width="100%" height="400")
          div#btn
            button#mute 🔊
            button#camera 🎥
        div#peerStream
          video#peerFace(autoplay, playsinline, width="100%" height="400")
      div#chat
        div
          ul
          form
            input(type="text", placeholder="message")
            button send
    script(src="/socket.io/socket.io.js")
    script(src="/public/js/app.js")